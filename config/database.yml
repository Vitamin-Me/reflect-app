# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: postgresql
  encoding: unicode
  username: dlowell
  password: '2058'
  host: localhost
  pool: 5

development:
  <<: *default
  database: reflect_app_development

test:
  <<: *default
  database: reflect_app_test

production:
  <<: *default
  database: reflect_app_production
  username: <%= ENV["PGUSER"] %>
  password: <%= ENV["PGPASSWORD"] %>

cable:
  <<: *default
  database: reflect_app_cable
